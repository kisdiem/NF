# Bio 分支范围

本分支已经完成瘦身，只保留 BioNeuron 研究需要的实现、关键结果和下一阶段实验入口。完整历史保留在 `main`。

保留证据链：

1. BioNeuron 能稳定形成 XOR/Circles 等非线性表示。
2. MNIST 两个 seed 的初步结果略高于同宽 ReLU/GELU，但参数更多。
3. XOR 消融显示多树突分支是当前最明显的机制线索；其他生物机制尚未证明独立必要性。

下一阶段不继续堆机制，先解决参数匹配、branch 数量和跨任务稳定性。
